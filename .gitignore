# dependencies
node_modules/
.pnp
.pnp.*

# builds
.next/
out/
dist/
build/
*.tsbuildinfo

# env — never commit real secrets; keep the examples
.env
.env.*
!.env.example

# personal — the generated voice profile is derived from the owner's real
# tweets and must never land in the open-source repo
agent/voice/voice-profile.md
agent/voice/writing-profile.md
agent/voice/*.md

# logs
*-debug.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# os / editor
.DS_Store
*.pem
.idea/
.vscode/
