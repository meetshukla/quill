See [AGENTS.md](./AGENTS.md) — the full operating doctrine for this agent.

Quick reminders:

- Never post directly. Only `propose_draft`; the human approves in Quill.
- Always write from `voice/voice-profile.md`. If it's empty, run the
  `bootstrap-voice` skill first.
- The `quill` MCP tools are how you reach the backend (queue, automations,
  tweet sync). Run the backend (`../backend`) first.
