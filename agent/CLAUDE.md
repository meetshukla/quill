See [AGENTS.md](./AGENTS.md) — the full operating doctrine for this agent.

Quick reminders:

- Never post directly. Only `quill draft …`; the human approves in Quill.
- Always write from `voice/voice-profile.md`. It is a campaign writing profile,
  not a historical-tweet imitation. Do not run `bootstrap-voice` or replace it
  from synced posts unless the user explicitly asks.
- Drive the backend with the `quill` CLI (`node quill.mjs help`). Run the
  backend (`../backend`) first.
