import type { BaseLayoutProps } from "fumadocs-ui/layouts/shared";

export const docsBaseOptions = (): BaseLayoutProps => ({
  nav: { title: "Quill MCP", url: "/docs" },
  themeSwitch: { enabled: false }
});
